# Copy to `.env.local` and fill in.

APP_ENV=local
PORT=8080
CORS_ORIGINS=["http://localhost:3000"]

# --- auth ---
# SESSION_SECRET must be >=32 chars. Rotate and all cookies invalidate.
SESSION_SECRET=change-me-to-a-long-random-string-please

# Seeds the first admin on boot if no users exist. Skip if already set up.
BOOTSTRAP_ADMIN_EMAIL=admin@localhost
BOOTSTRAP_ADMIN_PASSWORD=change-me-on-first-login-please

# --- db ---
DATABASE_URL=sqlite:///./app.db

# LLM
GEMINI_API_KEY=
GEMINI_CHAT_MODEL=gemini-2.5-flash
GEMINI_EMBED_MODEL=gemini-embedding-001

# Storage (empty GCS_BUCKET → local-fs fallback)
GCS_BUCKET=
LOCAL_STORAGE_DIR=./tmp/storage

# Pipeline
MAX_UPLOAD_MB=50
SQL_ROW_LIMIT=1000
SQL_TIMEOUT_SECONDS=30
MAX_REFINEMENT_ITERATIONS=2

# Paths
BUNDLED_DBS_DIR=./Databases
